{
  "name": "presenterkit",
  "version": "1.0.0",
  "description": "PresenterKit website",
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel --no-hmr --host 0.0.0.0",
    "build": "parcel build && cp -r static/* src/assets dist",
    "bundle": "rm -rf dist && parcel build && cp -r static/* src/assets dist"
  },
  "keywords": [],
  "author": "Ali Mammadli & Sevil Rasulova",
  "license": "ISC",
  "browserslist": "last 2 Chrome versions, last 2 Safari major versions, last 2 iOS major versions",
  "devDependencies": {
    "lil-gui": "^0.19.2",
    "parcel": "^2.12.0",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "posthog-js": "^1.154.2",
    "three": "^0.165.0"
  }
}
